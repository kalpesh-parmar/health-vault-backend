In my health Vault project
the folder structure i maintain and based on these folder structure in src folder
---api-docs:= representswagger yaml filese
---config:= for confog files
---Constant:= for constant variables like store predefine messages and constant errors
---controllers := handles request and responsed
---exception:= for error handling
---helper:= helper functions used multiple times in projects
---models:= represents schemas of Satabase
---prompt represents prompts to train ai model
---repositories:= all db queries
---routes:= all endpoints based on
---services:= business logic of projects
---validation:= zod validation for req.body
