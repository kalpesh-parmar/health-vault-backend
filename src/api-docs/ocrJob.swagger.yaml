openapi: 3.0.0
info:
  title: OCR Jobs API
  version: 1.0.0

components:
  schemas:
    OcrJobStatusResponse:
      type: object
      properties:
        id: { type: string, format: uuid }
        fileKey:
          {
            type: string,
            example: "documents/11111111-1111-1111-1111-111111111111/uuid-report.pdf",
          }
        userId: { type: string, format: uuid }
        status:
          {
            type: string,
            enum: [QUEUED, RUNNING, COMPLETED, FAILED, CANCELLED],
            example: "RUNNING",
          }
        stage: { type: string, example: "EXTRACTING" }
        percentage: { type: integer, example: 40 }
        currentStep: { type: string, example: "Extracting text" }
        completedSteps: { type: integer, example: 3 }
        pendingSteps: { type: integer, example: 5 }
        message: { type: string, nullable: true }
        metadata: { type: object }
        error: { type: string, nullable: true }
        startedAt: { type: string, format: date-time, nullable: true }
        completedAt: { type: string, format: date-time, nullable: true }

    OcrJobStartResponse:
      type: object
      properties:
        success: { type: boolean, example: true }
        statusCode: { type: integer, example: 202 }
        message: { type: string, example: "OCR job started" }
        data:
          type: object
          properties:
            jobId: { type: string, format: uuid }
            fileKey: { type: string }
            status: { type: string, example: "OCR_STARTED" }
            stage: { type: string, example: "OCR_QUEUED" }

    OcrJobResultResponse:
      type: object
      properties:
        success: { type: boolean, example: true }
        statusCode: { type: integer, example: 200 }
        message: { type: string, example: "OCR job result fetched" }
        data:
          type: object
          properties:
            jobId: { type: string, format: uuid }
            fileKey: { type: string }
            status: { type: string, example: "COMPLETED" }
            extractedStructuredData:
              type: object
              properties:
                patientInfo: { type: object }
                hospitalInfo: { type: object }
                doctorInfo: { type: object }
                diagnoses: { type: array, items: {} }
                medications: { type: array, items: {} }
                labResults: { type: array, items: {} }
                vitals: { type: array, items: {} }
                summaryEnglish: { type: string }
                summaryInPreferredLanguage: { type: string }
            summaries:
              type: object
              properties:
                summaryEnglish: { type: string }
                summaryInPreferredLanguage: { type: string }
            graphs:
              type: array
              items: { type: object }

paths:
  /ocr/jobs/{jobId}/start:
    post:
      tags: [OCR Jobs]
      summary: Start background OCR processing for a QUEUED job
      description: |
        Atomically claims a `QUEUED` OCR job by `jobId` (UUID) and triggers the background extraction pipeline.
        Returns 202 Accepted. Returns 400 if the job is already RUNNING or COMPLETED.
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: jobId
          required: true
          schema:
            type: string
            format: uuid
          description: OCR Job UUID
      responses:
        "202":
          description: OCR job claimed and pipeline started successfully
          content:
            application/json:
              schema: { $ref: "#/components/schemas/OcrJobStartResponse" }
        "400":
          description: Bad request (Job not in QUEUED status or invalid UUID)
        "401":
          description: Unauthorized (Token missing/expired or job owned by another user)
        "404":
          description: OCR job not found

  /ocr/jobs/{jobId}:
    get:
      tags: [OCR Jobs]
      summary: Fetch OCR job status and execution progress metrics
      description: Returns the latest execution status, stage, percentage, and step metrics for the specified jobId.
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: jobId
          required: true
          schema:
            type: string
            format: uuid
          description: OCR Job UUID
      responses:
        "200":
          description: OCR job status fetched successfully
          content:
            application/json:
              schema: { $ref: "#/components/schemas/OcrJobStatusResponse" }
        "400":
          description: Invalid request / invalid UUID format
        "401":
          description: Unauthorized
        "404":
          description: OCR job not found

  /ocr/jobs/{jobId}/result:
    get:
      tags: [OCR Jobs]
      summary: Fetch extracted medical data upon job completion
      description: |
        Returns extracted medical structured data, summaries, and graphs when status === COMPLETED.
        Returns 409 Conflict if the job is still QUEUED or RUNNING. Returns 400 Bad Request if the job FAILED or was CANCELLED.
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: jobId
          required: true
          schema:
            type: string
            format: uuid
          description: OCR Job UUID
      responses:
        "200":
          description: Medical extraction result fetched successfully
          content:
            application/json:
              schema: { $ref: "#/components/schemas/OcrJobResultResponse" }
        "400":
          description: Job failed or was cancelled
        "401":
          description: Unauthorized
        "404":
          description: OCR job not found
        "409":
          description: Conflict (OCR job is not ready yet; status is QUEUED or RUNNING)
