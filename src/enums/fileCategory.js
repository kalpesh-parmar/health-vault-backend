const FileCategory = Object.freeze({
  PROFILE: "profiles",
  DOCUMENT: "documents",
});

const fileCategoryValues = Object.values(FileCategory);

module.exports = { FileCategory, fileCategoryValues };
